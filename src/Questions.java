public class Questions {
    // (a) What is called first during an object creation, a static block or a constructor?
    // Ans==constructor
    //(b) How many times each (a constructor and a static block) is executed?
    // a static block execute only one time....(.class) execute then static block allocate the memory.
    // we can execute constructor more than one time
    //(c) Can we use non-static variables inside static methods? Why?
    // no we can not use non-static variables inside the static methods..(because for initializing the static variabal
    // we design the static method
}
